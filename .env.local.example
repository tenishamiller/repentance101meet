# Local-only secrets for Repentance 101 (never commit real values).
# Copy to .env.local if you need extra machine-specific overrides.

# Optional: only set if deploying under a specific Vercel team (NOT braid-appt)
# Leave blank to use your personal Vercel account
# VERCEL_SCOPE=
